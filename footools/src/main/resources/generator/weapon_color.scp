[itemdef ${defname}_${suffix}]
id=${defname}
name=${nameAccentless}
type=${type}

reqstr=${reqstr}
weight=${weight}

skillmake=${skillmake}
resources=${resources}

${tevents}
${tags}

category=${category}
subsection=${subsection}
description=${description}

on=@Create
    f_craft_hitpoints ${hitsMin},${hitsMax}
    ${nameu:-}
    color=${color}

